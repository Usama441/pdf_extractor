#!/bin/bash -e

# Remove stale server pid files when the app directory is mounted from the host.
rm -f /rails/tmp/pids/server.pid

# Prepare the database when requested by the container environment.
if [ "${RUN_DB_PREPARE:-false}" = "true" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
